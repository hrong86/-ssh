杀掉端口占用
lsof -i tcp:8081（注8081是端口号，你想看其它的话可以改）
kill进程（PID）
比如  kill 559


vi 打开当前文件
o 进入编辑模式
esc 退出编辑
:w 保存文件
:q 退出
:wq 保存退出
:x 同:wq
:q! 强行退出


安装nginx

/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)" 
brew install nginx	
